# 中级经济师大龄考生专属备考系统

## 项目定位
**「慢慢记」中级经济师大龄考生专属备考系统**，面向 28‑45 岁职场人士，解决记忆力下降、碎片化时间学习难题。系统提供 **PC、H5、微信小程序** 三端统一体验，围绕 **艾宾浩斯记忆曲线** + **大龄关怀 UI**，实现高效、轻松的备考流程。

## 核心功能
- **全渠道 Landing Page**：品牌 Hero、考证入口、核心优势快速引导。
- **专项备考看板**：仅展示中级经济师学科章节、复习进度、每日任务。
- **智能复盘引擎**：基于遗忘曲线自动生成复习计划，30 秒后弹出复习提醒。
- **全站考点搜索**：跨章节关键词检索，直接定位到对应章节和要点。
- **资料批量下载**：PC 端一次性下载教材、历年真题、答案解析。
- **模拟机考（CBT）**：45 分钟全真计时，错题自动归档。
- **会员体系**：免费/单科/全站通卡三级，支持微信支付快速升级。
- **大龄关怀 UI**：
  - 护眼暗色模式 + 暖色护眼配色
  - 字体大小弹性缩放（`Ctrl+滚轮` 或 UI 按钮）
  - 阅读计时与休息提醒，防止久坐疲劳。
- **教研实时同步**：PC 端编辑的章节内容实时推送至 H5 与小程序，保证三端数据一致。

## 技术实现概览
- **前端**：Vanilla JS + HTML + CSS（可迁移至 Vue3），采用统一设计系统（颜色、排版、微交互）
- **后端**：Node.js + Express，模块化业务（用户、题库、会员、复盘）
- **实时同步**：WebSocket / Firebase Realtime Database，实现 PC ↔ H5 ↔ 小程序即时数据同步。
- **支付**：微信支付 API（Web） + 小程序 JSSDK（wx.pay），统一订单中心。
- **缓存**：Redis 用于积分、学习进度短期缓存及限流。
- **持久化**：MongoDB 存储用户、章节、题库、会员信息。
- **部署**：Docker Compose + Nginx 静态托管，PM2 管理 Node 进程，MongoDB 与 Redis 持久化容器。

## 数据模型（关键片段）
```json
User {
  _id: ObjectId,
  username: String,
  passwordHash: String,
  membershipTier: Enum('free','single','vip'),
  points: Number,
  subjectsState: {
    econ: {
      progress: Number,
      completedChapters: [String],
      reviewSchedule: [{ questionId, nextReview }]
    }
  }
}
```

## 接口（REST + WS）
- `GET /api/subjects/econ/chapters` → 返回中级经济师章节列表
- `POST /api/econ/review/schedule` → 提交复盘结果，返回下次复习时间
- `POST /api/payment/create` → 生成微信统一下单（单科或全站通卡）
- `WebSocket /ws/econ/sync` → 实时推送学习进度、章节更新

## UI/UX 关键设计
1. **配色**：主色 HSL(210,45%,55%)，辅助色 HSL(340,35%,60%)，深色模式使用暗灰基底。
2. **动效**：章节卡片悬停微动、进度条渐变、题目切换淡入淡出。
3. **响应式**：Flex/Grid 布局，PC 三列展示，移动端单列卡片。
4. **无障碍**：ARIA 标签、键盘导航、字号可调（`font-size: var(--base-size)`）。
5. **护眼**：`prefers-color-scheme: dark` 自动切换，配合柔和背景纹理。

## 开发里程碑（对应《实现概览与后续开发计划》）
| 阶段 | 时间窗口 | 关键交付 |
|------|----------|----------|
| 1️⃣ 核心商业化 | 2026‑06‑01 ~ 2026‑07‑15 | 微信支付接入、三端数据同步、会员 UI 完稿 |
| 2️⃣ 活跃运营 | 2026‑07‑16 ~ 2026‑08‑31 | 社群积分体系、冲刺专项页面、运营后台报表 |
| 3️⃣ 内容导出 | 2026‑09‑01 ~ 2026‑10‑15 | PDF/EPUB 生成服务、下载/分享 UI |
| 4️⃣ 口碑系统 | 2026‑10‑16 ~ 2026‑11‑30 | 考生案例提交、审核、展示页 |

---

> 此文档已保存至 `d:\project\kaoshi\mid_economist_kaoshi_system.md`，供后续开发、评审与文档归档使用。
